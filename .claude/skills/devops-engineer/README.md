# DevOps Engineer skills — reserved

No skills are defined yet for this agent because the Implementation and
Maintenance phase (`sdlc-implementation`) is not yet in use, by design.

When this phase is specified in the future, add skill files here (e.g.
deployment checklists, rollback strategy, environment promotion rules) the
same way the other agents' skills are structured.
